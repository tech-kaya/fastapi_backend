version: 1.0
runtime: docker
build:
  commands:
    build:
      - cd fastapi_backend && docker build -t backend-kaya .
run:
  runtime-version: latest
  command: uvicorn app.main:app --host 0.0.0.0 --port 8000
  network:
    port: 8000
